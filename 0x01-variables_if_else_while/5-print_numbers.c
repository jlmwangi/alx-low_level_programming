#include <stdio.h>
/**
 * main - presents the entry
 *
 * Return: 0 (successful)
 */
int main(void)
{
	int a = 0;

		for (a = 0; a < 10; a++)
		{
			printf("%d", a);
		}
		printf("\n");
		return (0);
}
