#include <stdio.h>
/**
 * main - entry information
 *
 * Return: 0 (successful)
 */
int main(void)
{
	char z = 25;

		for (z = 25; z >= 'a'; z--)
		{
			putchar(z);
		}
	putchar('\n');
		return (0);
}
