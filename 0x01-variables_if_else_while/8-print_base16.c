#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0 (successful)
 */
int main(void)
{
	int n = '0';
	char letter = 'a';

	for (n = 0; n < 10; n++)
	{
		putchar(n);
	}
	for (char letter = a; letter <= f; letter++)
	{
		putchar(letter);
	}
	putchar('\n');
		return (0);
}
