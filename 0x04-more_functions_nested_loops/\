#include "main.h"
# include <stdio.h>
/**
 * main - prints fizz and buzz
 *
 * Return: 0(successful)
 */
int main(void)
{
	int i;
	int a = "FizzBuzz";
	int b = "Fizz";
	int c = "Buzz";

	for (i = 1; i <= 100; i++)
	{
		if (i % 5 == 0 && i % 3 == 0)
		{
			printf("%d", 'a');
		}
		else if (i % 3 == 0)
		{
			printf("%d", 'b');
		}

		else if (i % 5 == 0)
		{
			printf("%d", 'c');
		}
		else
		{
			printf("%d", i);
		}

		if (i != 100)
		{
			_putchar(' ');
		}
	}
	_putchar('\n');
	return (0);
}

